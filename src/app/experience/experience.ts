export interface Experience {
    startDate: string;
    endDate: string;
    image: string;
    title: string;
    name: string;
    description?: string;
    icon: string;
    longDescription: string;
}